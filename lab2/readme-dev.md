для проверки нужен был torch==1.0.1   
для него uv говорит нужен python 3.7  
uv поддерживает только python>=3.8.20  

будем делать на текущей версии torch  
в телеге есть замечания  
что поправить чтоб не ломать тест  
заменить x +=... на x = x + ... (хотя это вроде не относится к версии)